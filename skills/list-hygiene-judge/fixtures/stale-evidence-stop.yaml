name: list-hygiene-stale-evidence-stops-without-append
kind: skill
target: ..
runner: judge
inputs:
  data_source_ref: local://list-hygiene-judge/fixture-stale
  resource: contact_consent_events
  aggregate_id: contact:fixture-stale
  expected_version: 0
  idempotency_key: contact:fixture-stale:consent:v1
  engagement_history: { opens_count: 0, clicks_count: 0, hard_bounces: 0, recency_days: 180 }
  bounce_policy: { hard_bounce_action: suppress, decay_threshold_days: 90 }
  current_consent_state: { state: subscribed, active_unsubscribe_marker: false, evidence_status: stale, evidence_version: 0 }
expect:
  status: sealed
  steps: [read_contact, decide, readback, finalize]
  step_outputs:
    finalize:
      subset:
        list_hygiene_result:
          data:
            decision: { state: human_review }
            recorded_transition: { recorded: false, after_version: 0 }
  receipt: { schema: runx.receipt.v1 }
metadata: { public_skill: list-hygiene-judge, source_case: stale-evidence-stop, source: skills-fixture }
